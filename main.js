//setting variables
var text = "multiply";
var num = 3;

//multiplyig the string and number variable (results of nan)
console.log(text*num);

//console log adding 2 and true (results of  null or undefined)
console.log(2+true);

    //Homework 4_2--using math
    var counter = 2;
    var add = counter + 5;

//console log multiplying sum to 2 (7*2=14)
   console.log(add*2);

//the answer to the previous console log module by 3 (should be remainder 2)
console.log(14 % 3); //I guessed correctly--yey!



//New Math operation to test
var pink = 5;

//determines if the number is even and odd and gives the output and answer
Function evenOdd(num) {
    if num % 2 = 0,
    console.log(num % 2 + " -- " + "even");
    else if num % > 0,
    console.log(num % 2 + " -- " + "odd");
    else 
    console.log("error")
}

//creating conditional statements
<h1>"Do I move to Houston this year?"</h1>
var account;
var car;

if account => $3000 % car = true, then "yes";
else if account < $3000, | car = false,then "no"; 
else if account ~ < $5000, console.log ("yes")
else "error";